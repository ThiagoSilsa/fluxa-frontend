# 07: Importações — layout padrão de página

**What to build:** A página de importações passa a usar o layout padrão das demais telas: `PageLayout` com `Header` (título + descrição) e botões de subpágina no padrão (Button toggle, como portaria/blocks), mantendo as 4 modalidades de importação (departamentos, veículos, usuários, vínculo usuário-veículo).

**Blocked by:** None (can start immediately).

**Status:** ready-for-agent

- [ ] Página usa `PageLayout` + `Header` com título e descrição (i18n).
- [ ] Subpáginas (modalidades) apresentadas como botões no padrão das demais telas.
- [ ] Upload/template e histórico por modalidade continuam funcionando.
- [ ] Sem regressão visual nas demais telas.
