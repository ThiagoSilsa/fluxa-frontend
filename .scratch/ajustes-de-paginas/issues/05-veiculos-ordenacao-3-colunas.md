# 05: Veículos — ordenação apenas nas colunas suportadas

**What to build:** Na tela de veículos, apenas `plate`, `isActive` e `createdAt` aparecem como colunas ordenáveis/clicáveis — as demais não disparam ordenação (removendo o "clicar que não funciona").

**Blocked by:** None (can start immediately).

**Status:** ready-for-agent

- [ ] Apenas `plate`, `isActive`, `createdAt` ficam clicáveis/ordenáveis na tela de veículos.
- [ ] Cabeçalhos das demais colunas não sugerem ordenação.
- [ ] Ordenação continua alinhada ao contrato da API (`sortBy`/`sortOrder`).
